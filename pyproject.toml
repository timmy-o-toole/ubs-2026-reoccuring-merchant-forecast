[project]
name = "ubs-2026-recurring-merchant-forecast"
version = "1.0.0"
description = "SparseLevels: one sparse logistic regression per level (L1 or elastic net), forecasting a client's next recurring merchant family"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pandas",
    "scikit-learn>=1.0",
]
