[project]
name = "ubs-2026-recurring-merchant-forecast"
version = "1.0.0"
description = "Sparse per-label logistic regression (L1 / lasso) that forecasts a client's next recurring merchant family"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pandas",
    "scikit-learn>=1.0",
]
